{
  "name": "boss-os",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev:admin": "pnpm --filter admin-web-next dev",
    "dev:field": "pnpm --filter field-web-next dev",
    "dev:api": "pnpm --filter api dev",
    "build:admin": "pnpm --filter admin-web-next build",
    "build:field": "pnpm --filter field-web-next build",
    "build:api": "pnpm --filter api build",
    "db:generate": "pnpm --dir apps/api db:generate",
    "db:migrate": "pnpm --dir apps/api db:migrate",
    "db:seed-admin": "pnpm --dir apps/api db:seed-admin"
  }
}